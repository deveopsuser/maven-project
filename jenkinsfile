pipeline
{
agent any
stages
{stage('scm checkout')
{steps {git 'https://github.com/deveopsuser/maven-project'}
}
{ stage( 'code compile')
{steps{withMaven(globalMavenSettingsConfig: 'null', jdk: 'null', maven: 'maven-01', mavenSettingsConfig: 'null')
{ sh 'mvn compile' }
}
}
}
}
